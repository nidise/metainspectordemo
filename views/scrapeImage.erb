<% if @page.images.owner_suggested %>
    <%= @page.images.owner_suggested.to_json %>
<% else %>

    <% if @page.images.any? %>
        <%= @page.images.with_size.to_json %>
    <% end %>

<% end %>
